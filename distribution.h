#pragma once

#include "Student.h"

#include <chrono>
#include <algorithm>
#include <iterator>
#include <numeric>

void distribute(std::vector<Student> &student, std::vector<Student> &bad);
